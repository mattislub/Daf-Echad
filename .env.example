# Base URL for the remote application server (use https:// for TLS)
VITE_API_BASE_URL=https://api.example.com

# Optional healthcheck path on the remote server
VITE_API_HEALTHCHECK_PATH=/health

# Remote database connection details (do not commit real credentials)
VITE_REMOTE_DB_HOST=db.example.com
VITE_REMOTE_DB_PORT=5432
VITE_REMOTE_DB_NAME=daf_echad
VITE_REMOTE_DB_USER=readonly_user
VITE_REMOTE_DB_PASSWORD=change-me
